---
const enabled = import.meta.env.PUBLIC_VISUAL_EDITOR === "true";
---

{enabled && (
  <script is:inline>
    window.__vibeflowVisualEditor = window.__vibeflowVisualEditor || { enabled: true };
  </script>
)}
